//config file that allows certain types and fields to be easily updated and changed
loginAppModule.loginConfig = function($scope){
	//url for the web server hosting Aura Create
	$scope.serverRoot = "localhost";
}